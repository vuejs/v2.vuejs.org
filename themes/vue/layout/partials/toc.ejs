<ul class="menu-root">
  <% ['api', 'style-guide'].indexOf(type) === -1 && site.pages.find({type: type}).sort('order').each(function (p) { %>
    <% var fileName = p.path.replace(/^.+?\/([\w-]+)\.html/, '$1') %>
    <% if (type === 'guide') { %>
      <% if (fileName === 'installation') { %>
        <li><h3>Khái niệm cơ bản</h3></li>
      <% } %>
      <% if (fileName === 'transitions') { %>
        <li><h3>Hiệu ứng chuyển động</h3></li>
      <% } %>
      <% if (fileName === 'mixins') { %>
        <li><h3>Tái sử dụng & kết hợp</h3></li>
      <% } %>
      <% if (fileName === 'deployment') { %>
        <li><h3>Công cụ</h3></li>
      <% } %>
      <% if (fileName === 'routing') { %>
        <li><h3>Mở rộng quy mô ứng dụng</h3></li>
      <% } %>
      <% if (fileName === 'reactivity') { %>
        <li><h3>Bên trong Vue</h3></li>
      <% } %>
      <% if (fileName === 'migration') { %>
        <li><h3>Hướng dẫn nâng cấp</h3></li>
      <% } %>
      <% if (fileName === 'comparison') { %>
        <li><h3>Thông tin thêm</h3></li>
      <% } %>
    <% } %>
    <% if (type === 'cookbook') { %>
      <% if (p.order === 1.1) { %>
        <li><h3>Đối tượng Vue</h3></li>
      <% } %>
      <!-- Move cookbook sections here once they have -->
      <!-- at least one recipe -->
    <% } %>
    <li>
      <a href="<%- url_for(p.path) %>" class="sidebar-link<%- page.title === p.title ? ' current' : '' %><%- p.is_new ? ' new' : '' %>"><%- p.title %></a>
    </li>
  <% }) %>
  <% if (type === 'cookbook') { %>
    <li><h3>Component</h3></li>
    <li><h4>Form input</h4></li>
    <li><h4>Tích hợp với bên thứ 3</h4></li>
    <li><h3>Hiệu ứng chuyển đổi</h3></li>
    <li><h3>Directive</h3></li>
    <li><h3>Vue router</h3></li>
    <li><h3>Vuex</h3></li>
  <% } %>
</ul>
